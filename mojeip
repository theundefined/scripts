wget -q http://www.twojeip.wp.pl -O - | \
	egrep "ipadress =|ipfrom =" |awk '{printf "%s",$4}'
echo

host o-o.myaddr.l.google.com ns1.google.com
host myip.opendns.com resolver1.opendns.com

#	grep -A 2 bx_yip |
#	grep -v class |
#	cut -f 2 -d ">" |cut -f 1 -d "<" |xargs echo

curl ifconfig.co

curl https://api.ipify.org
